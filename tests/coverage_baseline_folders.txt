# Per-folder full-suite line-coverage FLOOR (ci_coverage_folders.m).
# No-regression ratchet: each folder must hold its baselined rate within a
# single tolerance TOL (in the script). Values are the exact measured rate on
# the pinned release; one TOL (not a rounded floor *and* a tolerance) absorbs
# the ~0.3-0.6pp run-to-run jitter on the correctness-path buckets (lib/@cada,
# lib/@cadastruct, lib) — their instrumented-line set shifts with which
# overloads a run touches. Regenerate with: ci_coverage_folders('write').
# Correctness-path rates are RAISED as the #38/#103 value oracles land.
# Measured on ubuntu-latest / MATLAB latest (Extended full-products job).
embedding          0.9182
util               0.7491
lib/cadaUtils      0.8667
lib                0.5933
lib/@cada          0.4030
lib/@cadastruct    0.1675
